// @ts-check
/**
 * #diff
 * @param {*} node 
 * @param {*} prevNode 
 * @returns {{attributeChanged, removed, added, textChanged, reordered}}
 */
const diff = (node, prevNode) => {

}

export default diff
